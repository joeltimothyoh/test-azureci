# templates/steps.yml

steps:
- bash: |
    pwd
    git status
    ls -al
    git clone https://github.com/joeltimothyoh/Get-SMARTReport.git

- powershell: |
    # git clone "https://github.com/joeltimothyoh/$projectName.git" 2>&1
    Get-Location
    Push-Location $projectName
    git status
    # Import-Module "Modules/$projectName/Get-SMARTReport.psm1"
    # Get-SMARTReport
