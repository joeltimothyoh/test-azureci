trigger:
  branches:
    include:
    - '*'
  tags:
    include:
    - '*'
pr: none

resources:
  repositories:
  - repository: PSModulePublisher
    type: github
    name: theohbrothers/PSModulePublisher
    endpoint: joeltimothyoh
    ref: refs/heads/dev

stages:
- stage: Build_Test
  displayName: Build, Test
  jobs:
  - job: windows2019
    pool:
      vmImage: windows-2019
    steps:
    - checkout: self
      submodules: true
    - pwsh: |
        Get-ChildItem -Recurse
      displayName: Display project paths
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        $elevated = ([Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)
        "Elevated: $elevated"
      displayName: Check for elevation (pwsh)
    - powershell: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        $elevated = ([Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)
        "Elevated: $elevated"
      displayName: Check for elevation (powershell)
    - pwsh: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check pre-installed package providers (pwsh)
    - powershell: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check pre-installed package providers (powershell)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check pre-installed modules (pwsh)
    - pwsh: |
        # "Installing dotnet" | Write-Host
        # Invoke-WebRequest -Uri 'https://dot.net/v1/dotnet-install.ps1' -OutFile '.\dotnet-install.ps1'; & '.\dotnet-install.ps1' -Channel Current -Version '2.0.0'*
        "Invoking dotnet" | Write-Host
        dotnet --info
      displayName: Install dotnet (pwsh)
    - powershell: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check pre-installed modules (powershell)
    # - pwsh: |
    #     "Installing Nuget" | Write-Host
    #     Install-PackageProvider -Name NuGet -MinimumVersion 2.8.5.201 -Force
    #     Get-PackageProvider NuGet | Select *
    #   displayName: Install package provider (pwsh)
    # - powershell: |
    #     "Installing Nuget" | Write-Host
    #     Install-PackageProvider -Name NuGet -MinimumVersion 2.8.5.201 -Force
    #     Get-PackageProvider NuGet | Select *
    #   displayName: Install package provider (powershell)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        "Installing PowerShellGet" | Write-Host
        $powershellGetRequiredVersion = '2.1.2'
        $powershellGetInstalledVersions = (Get-Module 'PowerShellGet' -ListAvailable).Version | % { $_.ToString() }
        if ($powershellGetRequiredVersion -notin $powershellGetInstalledVersions) {
            Install-Module -Name 'PowershellGet' -Repository 'PSGallery' -RequiredVersion $powershellGetRequiredVersion -Scope CurrentUser -Force
        }
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Install modules (pwsh)
    - powershell: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        "Installing PowerShellGet" | Write-Host
        $powershellGetRequiredVersion = '2.1.2'
        $powershellGetInstalledVersions = (Get-Module 'PowerShellGet' -ListAvailable).Version | % { $_.ToString() }
        if ($powershellGetRequiredVersion -notin $powershellGetInstalledVersions) {
            Install-Module -Name 'PowershellGet' -Repository 'PSGallery' -RequiredVersion $powershellGetRequiredVersion -Scope CurrentUser -Force
        }
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Install modules (powershell)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        "Checking for NuGet binary"
        $nugetBinPath = "$env:LOCALAPPDATA\Microsoft\Windows\PowerShell\PowerShellGet\NuGet.exe"
        if (!(Test-Path -Path $nugetBinPath -PathType Leaf)) {
            Invoke-WebRequest -Uri https://dist.nuget.org/win-x86-commandline/latest/nuget.exe -OutFile $nugetBinPath
        }
        Get-ChildItem -Path (Split-Path -Path $nugetBinPath -Parent) -ErrorAction SilentlyContinue
        & $nugetBinPath
      displayName: Install publishing dependencies (pwsh)
    - pwsh: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check installed package providers (pwsh)
    - powershell: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check installed package providers (powershell)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check installed modules (pwsh)
    - powershell: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check installed modules (powershell)
    # - template: azure-pipelines/windows/continuous-build.yml@PSModulePublisher
  - job: vs2017win2016
    pool:
      vmImage: vs2017-win2016
    steps:
    - checkout: self
      submodules: true
    - pwsh: |
        Get-ChildItem -Recurse
      displayName: Display project paths
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        $elevated = ([Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)
        "Elevated: $elevated"
      displayName: Check for elevation (pwsh)
    - powershell: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        $elevated = ([Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)
        "Elevated: $elevated"
      displayName: Check for elevation (powershell)
    - pwsh: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check pre-installed package providers (pwsh)
    - powershell: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check pre-installed package providers (powershell)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check pre-installed modules (pwsh)
    - pwsh: |
        # "Installing dotnet" | Write-Host
        # Invoke-WebRequest -Uri 'https://dot.net/v1/dotnet-install.ps1' -OutFile '.\dotnet-install.ps1'; & '.\dotnet-install.ps1' -Channel Current -Version '2.0.0'*
        "Invoking dotnet" | Write-Host
        dotnet --info
      displayName: Install dotnet (pwsh)
    - powershell: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check pre-installed modules (powershell)
    # - pwsh: |
    #     "Installing Nuget" | Write-Host
    #     Install-PackageProvider -Name NuGet -MinimumVersion 2.8.5.201 -Force
    #     Get-PackageProvider NuGet | Select *
    #   displayName: Install package provider (pwsh)
    # - powershell: |
    #     "Installing Nuget" | Write-Host
    #     Install-PackageProvider -Name NuGet -MinimumVersion 2.8.5.201 -Force
    #     Get-PackageProvider NuGet | Select *
    #   displayName: Install package provider (powershell)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        "Installing PowerShellGet" | Write-Host
        $powershellGetRequiredVersion = '2.1.2'
        $powershellGetInstalledVersions = (Get-Module 'PowerShellGet' -ListAvailable).Version | % { $_.ToString() }
        if ($powershellGetRequiredVersion -notin $powershellGetInstalledVersions) {
            Install-Module -Name 'PowershellGet' -Repository 'PSGallery' -RequiredVersion $powershellGetRequiredVersion -Scope CurrentUser -Force
        }
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Install modules (pwsh)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        "Checking for NuGet binary"
        $nugetBinPath = "$env:LOCALAPPDATA\Microsoft\Windows\PowerShell\PowerShellGet\NuGet.exe"
        if (!(Test-Path -Path $nugetBinPath -PathType Leaf)) {
            Invoke-WebRequest -Uri https://dist.nuget.org/win-x86-commandline/latest/nuget.exe -OutFile $nugetBinPath
        }
        Get-ChildItem -Path (Split-Path -Path $nugetBinPath -Parent) -ErrorAction SilentlyContinue
        & $nugetBinPath
      displayName: Install publishing dependencies (pwsh)
    - powershell: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        "Installing PowerShellGet" | Write-Host
        $powershellGetRequiredVersion = '2.1.2'
        $powershellGetInstalledVersions = (Get-Module 'PowerShellGet' -ListAvailable).Version | % { $_.ToString() }
        if ($powershellGetRequiredVersion -notin $powershellGetInstalledVersions) {
            Install-Module -Name 'PowershellGet' -Repository 'PSGallery' -RequiredVersion $powershellGetRequiredVersion -Scope CurrentUser -Force
        }
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Install modules (powershell)
    - pwsh: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check installed package providers (pwsh)
    - powershell: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check installed package providers (powershell)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check installed modules (pwsh)
    - powershell: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check installed modules (powershell)
    # - template: azure-pipelines/windows/continuous-build.yml@PSModulePublisher
  - job: win1803
    pool:
      vmImage: win1803
    steps:
    - checkout: self
      submodules: true
    - pwsh: |
        Get-ChildItem -Recurse
      displayName: Display project paths
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        $elevated = ([Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)
        "Elevated: $elevated"
      displayName: Check for elevation (pwsh)
    - powershell: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        $elevated = ([Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)
        "Elevated: $elevated"
      displayName: Check for elevation (powershell)
    - pwsh: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check pre-installed package providers (pwsh)
    - powershell: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check pre-installed package providers (powershell)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check pre-installed modules (pwsh)
    - pwsh: |
        # "Installing dotnet" | Write-Host
        # Invoke-WebRequest -Uri 'https://dot.net/v1/dotnet-install.ps1' -OutFile '.\dotnet-install.ps1'; & '.\dotnet-install.ps1' -Channel Current -Version '2.0.0'*
        "Invoking dotnet" | Write-Host
        dotnet --info
      displayName: Install dotnet (pwsh)
    - powershell: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check pre-installed modules (powershell)
    # - pwsh: |
    #     "Installing Nuget" | Write-Host
    #     Install-PackageProvider -Name NuGet -MinimumVersion 2.8.5.201 -Force
    #     Get-PackageProvider NuGet | Select *
    #   displayName: Install package provider (pwsh)
    # - powershell: |
    #     "Installing Nuget" | Write-Host
    #     Install-PackageProvider -Name NuGet -MinimumVersion 2.8.5.201 -Force
    #     Get-PackageProvider NuGet | Select *
    #   displayName: Install package provider (powershell)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        "Installing PowerShellGet" | Write-Host
        $powershellGetRequiredVersion = '2.1.2'
        $powershellGetInstalledVersions = (Get-Module 'PowerShellGet' -ListAvailable).Version | % { $_.ToString() }
        if ($powershellGetRequiredVersion -notin $powershellGetInstalledVersions) {
            Install-Module -Name 'PowershellGet' -Repository 'PSGallery' -RequiredVersion $powershellGetRequiredVersion -Scope CurrentUser -Force
        }
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Install modules (pwsh)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        "Checking for NuGet binary"
        $nugetBinPath = "$env:LOCALAPPDATA\Microsoft\Windows\PowerShell\PowerShellGet\NuGet.exe"
        if (!(Test-Path -Path $nugetBinPath -PathType Leaf)) {
            Invoke-WebRequest -Uri https://dist.nuget.org/win-x86-commandline/latest/nuget.exe -OutFile $nugetBinPath
        }
        Get-ChildItem -Path (Split-Path -Path $nugetBinPath -Parent) -ErrorAction SilentlyContinue
        & $nugetBinPath
      displayName: Install publishing dependencies (pwsh)
    - powershell: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        "Installing PowerShellGet" | Write-Host
        $powershellGetRequiredVersion = '2.1.2'
        $powershellGetInstalledVersions = (Get-Module 'PowerShellGet' -ListAvailable).Version | % { $_.ToString() }
        if ($powershellGetRequiredVersion -notin $powershellGetInstalledVersions) {
            Install-Module -Name 'PowershellGet' -Repository 'PSGallery' -RequiredVersion $powershellGetRequiredVersion -Scope CurrentUser -Force
        }
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Install modules (powershell)
    - pwsh: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check installed package providers (pwsh)
    - powershell: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check installed package providers (powershell)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check installed modules (pwsh)
    - powershell: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check installed modules (powershell)
    # - template: azure-pipelines/windows/continuous-build.yml@PSModulePublisher
  - job: vs2015win2012r2
    pool:
      vmImage: vs2015-win2012r2
    steps:
    - checkout: self
      submodules: true
    - powershell: |
        Get-ChildItem -Recurse
      displayName: Display project paths
    # - pwsh: |
    #     $ErrorActionPreference = 'Stop'
    #     $VerbosePreference = 'Continue'
    #     $elevated = ([Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)
    #     "Elevated: $elevated"
    #   displayName: Check for elevation (pwsh)
    - powershell: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        $elevated = ([Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)
        "Elevated: $elevated"
      displayName: Check for elevation (powershell)
    # - pwsh: |
    #     Get-PackageProvider -ListAvailable
    #     Get-PackageProvider -ListAvailable | Select *
    #   displayName: Check pre-installed package providers (pwsh)
    - powershell: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check pre-installed package providers (powershell)
    # - pwsh: |
    #     $ErrorActionPreference = 'Stop'
    #     $VerbosePreference = 'Continue'
    #     Get-Module PowerShellGet -ListAvailable
    #     Get-Module PackageManagement -ListAvailable
    #     Get-Module PowerShellGet -ListAvailable | Select *
    #     Get-Module PackageManagement -ListAvailable | Select *
    #   displayName: Check pre-installed modules (pwsh)
    - powershell: |
        # "Installing dotnet" | Write-Host
        # Invoke-WebRequest -Uri 'https://dot.net/v1/dotnet-install.ps1' -OutFile '.\dotnet-install.ps1'; & '.\dotnet-install.ps1' -Channel Current -Version '2.0.0'*
        "Invoking dotnet" | Write-Host
        dotnet --info
      displayName: Install dotnet (powershell)
    - powershell: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check pre-installed modules (powershell)
    # - pwsh: |
    #     "Installing Nuget" | Write-Host
    #     Install-PackageProvider -Name NuGet -MinimumVersion 2.8.5.201 -Force
    #     Get-PackageProvider NuGet | Select *
    #   displayName: Install package provider (pwsh)
    - powershell: |
        "Installing Nuget" | Write-Host
        Install-PackageProvider -Name NuGet -MinimumVersion 2.8.5.201 -Force
        Get-PackageProvider NuGet | Select *
      displayName: Install package provider (powershell)
    # - pwsh: |
    #     $ErrorActionPreference = 'Stop'
    #     $VerbosePreference = 'Continue'
    #     "Installing PowerShellGet" | Write-Host
    #     $powershellGetRequiredVersion = '2.1.2'
    #     $powershellGetInstalledVersions = (Get-Module 'PowerShellGet' -ListAvailable).Version | % { $_.ToString() }
    #     if ($powershellGetRequiredVersion -notin $powershellGetInstalledVersions) {
    #         Install-Module -Name 'PowershellGet' -Repository 'PSGallery' -RequiredVersion $powershellGetRequiredVersion -Scope CurrentUser -Force
    #     }
    #     Get-Module PowerShellGet -ListAvailable
    #     Get-Module PackageManagement -ListAvailable
    #     Get-Module PowerShellGet -ListAvailable | Select *
    #     Get-Module PackageManagement -ListAvailable | Select *
    #   displayName: Install modules (pwsh)
    - powershell: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        "Installing PowerShellGet" | Write-Host
        $powershellGetRequiredVersion = '2.1.2'
        $powershellGetInstalledVersions = (Get-Module 'PowerShellGet' -ListAvailable).Version | % { $_.ToString() }
        if ($powershellGetRequiredVersion -notin $powershellGetInstalledVersions) {
            Install-Module -Name 'PowershellGet' -Repository 'PSGallery' -RequiredVersion $powershellGetRequiredVersion -Scope CurrentUser -Force
        }
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Install modules (powershell)
    - powershell: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        "Checking for NuGet binary"
        $nugetBinPath = "$env:LOCALAPPDATA\Microsoft\Windows\PowerShell\PowerShellGet\NuGet.exe"
        if (!(Test-Path -Path $nugetBinPath -PathType Leaf)) {
            Invoke-WebRequest -Uri https://dist.nuget.org/win-x86-commandline/latest/nuget.exe -OutFile $nugetBinPath
        }
        Get-ChildItem -Path (Split-Path -Path $nugetBinPath -Parent) -ErrorAction SilentlyContinue
        & $nugetBinPath
      displayName: Install publishing dependencies (powershell)
    # - pwsh: |
    #     Get-PackageProvider -ListAvailable
    #     Get-PackageProvider -ListAvailable | Select *
    #   displayName: Check installed package providers (pwsh)
    - powershell: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check installed package providers (powershell)
    # - pwsh: |
    #     $ErrorActionPreference = 'Stop'
    #     $VerbosePreference = 'Continue'
    #     Get-Module PowerShellGet -ListAvailable
    #     Get-Module PackageManagement -ListAvailable
    #     Get-Module PowerShellGet -ListAvailable | Select *
    #     Get-Module PackageManagement -ListAvailable | Select *
    #   displayName: Check installed modules (pwsh)
    - powershell: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check installed modules (powershell)
    # - template: azure-pipelines/windows/continuous-build.yml@PSModulePublisher
  - job: ubuntu1604
    pool:
      vmImage: ubuntu-16.04
    # container: joeltimothyoh/powershell:6.1.0-ubuntu-18.04-git
    steps:
    - checkout: self
      submodules: true
    - pwsh: |
        Get-ChildItem -Recurse
      displayName: Display project paths
    - pwsh: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check pre-installed package providers (pwsh)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check pre-installed modules (pwsh)
    - pwsh: |
        # "Installing dotnet" | Write-Host
        # Invoke-WebRequest -Uri 'https://dot.net/v1/dotnet-install.ps1' -OutFile '.\dotnet-install.ps1'; & '.\dotnet-install.ps1' -Channel Current -Version '2.0.0'*
        "Invoking dotnet" | Write-Host
        dotnet --info
      displayName: Install dotnet (pwsh)
    - pwsh: |
        "Installing Nuget" | Write-Host
        Install-PackageProvider -Name NuGet -MinimumVersion 2.8.5.201 -Force
        Get-PackageProvider NuGet | Select *
      displayName: Install package provider (pwsh)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        "Installing PowerShellGet" | Write-Host
        $powershellGetRequiredVersion = '2.1.2'
        $powershellGetInstalledVersions = (Get-Module 'PowerShellGet' -ListAvailable).Version | % { $_.ToString() }
        if ($powershellGetRequiredVersion -notin $powershellGetInstalledVersions) {
            Install-Module -Name 'PowershellGet' -Repository 'PSGallery' -RequiredVersion $powershellGetRequiredVersion -Scope CurrentUser -Force
        }
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Install modules (pwsh)
    - pwsh: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check installed package providers (pwsh)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check installed modules (pwsh)
    # - template: azure-pipelines/linux/continuous-build.yml@PSModulePublisher
  - job: macOS1014
    pool:
      vmImage: macOS-10.14
    steps:
    - checkout: self
      submodules: true
    - pwsh: |
        Get-ChildItem -Recurse
      displayName: Display project paths
    - pwsh: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check pre-installed package providers (pwsh)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check pre-installed modules (pwsh)
    - pwsh: |
        # "Installing dotnet" | Write-Host
        # Invoke-WebRequest -Uri 'https://dot.net/v1/dotnet-install.ps1' -OutFile '.\dotnet-install.ps1'; & '.\dotnet-install.ps1' -Channel Current -Version '2.0.0'*
        "Invoking dotnet" | Write-Host
        dotnet --info
      displayName: Install dotnet (pwsh)
    - pwsh: |
        "Installing Nuget" | Write-Host
        Install-PackageProvider -Name NuGet -MinimumVersion 2.8.5.201 -Force
        Get-PackageProvider NuGet | Select *
      displayName: Install package provider (pwsh)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        "Installing PowerShellGet" | Write-Host
        $powershellGetRequiredVersion = '2.1.2'
        $powershellGetInstalledVersions = (Get-Module 'PowerShellGet' -ListAvailable).Version | % { $_.ToString() }
        if ($powershellGetRequiredVersion -notin $powershellGetInstalledVersions) {
            Install-Module -Name 'PowershellGet' -Repository 'PSGallery' -RequiredVersion $powershellGetRequiredVersion -Scope CurrentUser -Force
        }
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Install modules (pwsh)
    - pwsh: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check installed package providers (pwsh)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check installed modules (pwsh)
    # - template: azure-pipelines/macos/continuous-build.yml@PSModulePublisher
  - job: macOS1013
    pool:
      vmImage: macOS-10.13
    steps:
    - checkout: self
      submodules: true
    - pwsh: |
        Get-ChildItem -Recurse
      displayName: Display project paths
    - pwsh: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check pre-installed package providers (pwsh)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check pre-installed modules (pwsh)
    - pwsh: |
        # "Installing dotnet" | Write-Host
        # Invoke-WebRequest -Uri 'https://dot.net/v1/dotnet-install.ps1' -OutFile '.\dotnet-install.ps1'; & '.\dotnet-install.ps1' -Channel Current -Version '2.0.0'*
        "Invoking dotnet" | Write-Host
        dotnet --info
      displayName: Install dotnet (pwsh)
    - pwsh: |
        "Installing Nuget" | Write-Host
        Install-PackageProvider -Name NuGet -MinimumVersion 2.8.5.201 -Force
        Get-PackageProvider NuGet | Select *
      displayName: Install package provider (pwsh)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        "Installing PowerShellGet" | Write-Host
        $powershellGetRequiredVersion = '2.1.2'
        $powershellGetInstalledVersions = (Get-Module 'PowerShellGet' -ListAvailable).Version | % { $_.ToString() }
        if ($powershellGetRequiredVersion -notin $powershellGetInstalledVersions) {
            Install-Module -Name 'PowershellGet' -Repository 'PSGallery' -RequiredVersion $powershellGetRequiredVersion -Scope CurrentUser -Force
        }
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Install modules (pwsh)
    - pwsh: |
        Get-PackageProvider -ListAvailable
        Get-PackageProvider -ListAvailable | Select *
      displayName: Check installed package providers (pwsh)
    - pwsh: |
        $ErrorActionPreference = 'Stop'
        $VerbosePreference = 'Continue'
        Get-Module PowerShellGet -ListAvailable
        Get-Module PackageManagement -ListAvailable
        Get-Module PowerShellGet -ListAvailable | Select *
        Get-Module PackageManagement -ListAvailable | Select *
      displayName: Check installed modules (pwsh)
    # - template: azure-pipelines/macos/continuous-build.yml@PSModulePublisher
# - stage: Publish
#   # dependsOn:
#   # - Build_Test
#   jobs:
#   - job: Windows
#     pool:
#       vmImage: windows-2019
#     steps:
#     - checkout: self
#       submodules: true
#     - template: azure-pipelines/windows/continuous-build.yml@PSModulePublisher
#     - template: azure-pipelines/common/run-publish.yml@PSModulePublisher
